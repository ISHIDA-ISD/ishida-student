specVersion: "v1beta"
serviceId: "ishida-student"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ishida-student-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
